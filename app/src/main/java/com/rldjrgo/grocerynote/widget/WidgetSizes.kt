package com.rldjrgo.grocerynote.widget

import androidx.compose.ui.unit.DpSize
import androidx.compose.ui.unit.dp

object WidgetSizes {
    val Small = DpSize(110.dp, 110.dp)    // 2x2
    val Medium = DpSize(250.dp, 110.dp)   // 4x2 ★ default
    val Large = DpSize(250.dp, 250.dp)    // 4x4
}
